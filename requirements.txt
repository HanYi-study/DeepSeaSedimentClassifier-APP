# ============================================================
# DeepSeaSedimentClassifier 依赖清单
# ============================================================
# 安装: pip install -r requirements.txt
#
# 核心依赖 (必需)
# ------------------------------------------------------------

# GUI 框架
PyQt5>=5.15.0

# 数值计算
numpy>=1.21.0

# 可视化
matplotlib>=3.5.0

# 深度学习框架 (CPU 版，如需 GPU 加速请改为 torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118)
torch>=2.0.0

# 机器学习 (K-Means 伪标签)
scikit-learn>=1.0.0

# ============================================================
# 可选依赖 (按需安装)
# ============================================================

# 远程 GPU 连接
paramiko>=3.0.0

# DEM GeoTIFF 支持
# rasterio>=1.3.0

# SEG-Y 剖面格式支持
# segyio>=1.9.0

# Shapefile 导出支持
# fiona>=1.9.0

# GPU 加速 (CUDA 11.8)
# 如果安装了支持 CUDA 的 PyTorch，取消下面注释:
# torch>=2.0.0+cu118 --index-url https://download.pytorch.org/whl/cu118

# ============================================================
# 开发依赖 (仅开发者需要)
# ============================================================
# pytest>=7.0.0
# black>=23.0.0
